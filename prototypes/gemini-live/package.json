{
  "name": "@neura/gemini-live",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "test": "vitest run"
  },
  "dependencies": {
    "@google/genai": "^1",
    "dotenv": "^16",
    "express": "^4",
    "ws": "^8"
  },
  "devDependencies": {
    "@types/express": "^5",
    "@types/ws": "^8",
    "tsx": "^4"
  }
}
